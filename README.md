# tiny-tool-agent
This project is a simple tool-using AI agent built using Python.  The agent accepts natural language weather-related queries from the user, detects the intent, extracts the city name, and uses a weather tool to fetch live weather data from the OpenWeatherMap API.
